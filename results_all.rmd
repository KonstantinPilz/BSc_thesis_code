---
title: "R Notebook"
output: html_document
---
```{r, eval = FALSE}
## ----setup, echo=FALSE, results="hide"----------------------------------------
knitr::opts_chunk$set(tidy = FALSE,
                      cache = FALSE,
                      dev = "png",
                      message = FALSE,
                      error = FALSE,
                      warning = TRUE)
```
1. load libraries

```{r}
library("ggplot2")
library("tidyverse")
library("ggrepel")
library("DESeq2")
library("pheatmap")
```
3. define paths
```{r}
local <- 1
prefix <- ifelse(local, "~", "")

results_dir <- file.path(prefix, "data/bioinf/projects/data/2022_MLseq_KP/results")

dir_cervicalD <- file.path(prefix, "data/bioinf/projects/data/2022_MLseq_KP/cervical_DESeq2/results/")
dir_cervicalM <- file.path(prefix, "data/bioinf/projects/data/2022_MLseq_KP/cervical_MLSeq/results/")
dir_lungD <- file.path(prefix, "data/bioinf/projects/data/2022_MLseq_KP/lung_DESeq2/results/")
dir_lungM <- file.path(prefix, "data/bioinf/projects/data/2022_MLseq_KP/lung_MLSeq/results/")
dir_MAITD <- file.path(prefix, "data/bioinf/projects/data/2022_MLseq_KP/MAIT_DESeq2/results/")
dir_MAITM <- file.path(prefix, "data/bioinf/projects/data/2022_MLseq_KP/MAIT_MLSeq/results/")


```
4. load cervical data
```{r}
recompute <- 1
if(recompute){

  #results <- read.csv(file.path(dir_cervicalD, "07_results_cervical_all.csv"))
  results <- read.csv(file.path(dir_cervicalD, "05_results_cervical_p01_no_exclusion.csv"))
  results
  load(file.path(dir_cervicalM, "07_pam.rda"))
  load(file.path(dir_cervicalM, "08_plda.rda"))
  load(file.path(dir_cervicalM, "09_plda2.rda"))
  load(file.path(dir_cervicalM, "10_vnsc.rda"))

  genes <- genes.pam
  #create new column with 0
  results$MLSeq <- 0
  results$NSC <- 0
  #iterate through all genes
  for(gene in genes){

    #if genes.pam contains gene, set to 1
    pos <- which(results == gene)
    results[pos,8] <- results[pos,8] + 1
    results[pos,9] <- results[pos,9] + 1
  }

  results$PLDA <- 0
  genes <- genes.plda
  for(gene in genes){
    pos <- which(results == gene)
    results[pos,8] <- results[pos,8] + 1
    results[pos, 10] <- results[pos, 10] +1
  }
  genes <- genes.plda2
  results$PLDA2 <- 0
  for(gene in genes){
    pos <- which(results == gene)
    results[pos,8] <- results[pos,8] + 1
    results[pos,11] <- results[pos,11] + 1
  }
  genes <- genes.vnsc
  results$voomNSC <- 0
  for(gene in genes){
    pos <- which(results == gene)
    results[pos,8] <- results[pos,8] + 1
    results[pos,12] <- results[pos,12] + 1
  }

  results$MLSeq <- as.factor(results$MLSeq)
  results$NSC <- as.factor(results$NSC)
  results$PLDA <- as.factor(results$PLDA)
  results$PLDA2 <- as.factor(results$PLDA2)
  results$voomNSC <- as.factor(results$voomNSC)

}
```
5. plot cervical data

```{r}
if(recompute){

  ggplot(data=results, aes(x=log2FoldChange, y=-log10(pvalue), col=MLSeq)) +
          geom_point() +
          theme_minimal() +
          #geom_text_repel() +
          scale_color_manual(values=c("grey", "orange", "red", "purple")) +
          geom_vline(xintercept=c(-1, 1), col="red") +
          geom_hline(yintercept=-log10(0.05), col="red")

  ggplot(data=results, aes(x=MLSeq, y=-log10(pvalue))) +
          geom_point() +
          theme_minimal() +
          #geom_text_repel() +
          scale_color_manual(values=c("grey", "orange", "red", "purple")) +
          geom_vline(xintercept=c(-1, 1), col="red") +
          geom_hline(yintercept=-log10(0.05), col="red")
}
```
heatmap cervical
```{r}
#from DESeq2 vignette
#load dds
load( file= file.path(dir_cervicalD, "02_rld.Rdata"))
rld
load( file= file.path(dir_cervicalD, "04_dds.Rdata"))
dds
select <- order(rowMeans(counts(dds,normalized=TRUE)),
                decreasing=TRUE)[1:2]
df <- as.data.frame(colData(dds)[,c("condition")])
row <- results
rownames(row) <- row[,1]
#row <- (row)[select,]
temp <- as.data.frame(assay(rld[select]))
temp$MLSeq <- 0

a <- 0
list <- row.names(temp)
a <- 0
for (gene in list){
  a <- a + 1
  pos <- which(results == gene)
  print(gene)
  print(results[pos,8])
  temp[a,59] <- results[pos,8]
  #use variable that increases
  #copy value from results to temp
}
#temp <- temp[,59]


#How do I get the MLSeq data into the dds?
pheatmap(assay(rld)[select,], cluster_rows=FALSE, show_rownames=FALSE,
         cluster_cols=FALSE, annotation_col=df, annotation_row = temp$MLSeq)
```
lung
```{r}
recompute <- 0
if(recompute){

results <- read.csv(file.path(dir_lungD, "07_results_all.csv"))
results
load(file.path(dir_lungM, "07_pam.rda"))
load(file.path(dir_lungM, "08_plda.rda"))
load(file.path(dir_lungM, "09_plda2.rda"))
load(file.path(dir_lungM, "10_vnsc.rda"))

genes <- genes.pam
#create new column with 0
results$MLSeq <- 0
#iterate through all genes
for(gene in genes){

    #if genes.pam contains gene, set to 1
    pos <- which(results == gene)
    results[pos,8] <- results[pos,8] + 1
}
genes <- genes.plda
for(gene in genes){
pos <- which(results == gene)
results[pos,8] <- results[pos,8] + 1
}
genes <- genes.plda2

for(gene in genes){
pos <- which(results == gene)
results[pos,8] <- results[pos,8] + 1
}
genes <- genes.vnsc

for(gene in genes){
pos <- which(results == gene)
results[pos,8] <- results[pos,8] + 1
}

results$MLSeq <- as.factor(results$MLSeq)
  results[1:39, 6] <- 2.23e-308

  save(results, file = file.path(results_dir, "lung_table.Rdata"))
} else{
  load(file = file.path(results_dir, "lung_table.Rdata"))
}
```
plot lung data

```{r}
recompute <- 1
if(recompute){


  ggplot(data=results, aes(x=log2FoldChange, y=-log10(pvalue), col=MLSeq)) +
          geom_point() +
          theme_minimal() +
          #geom_text_repel() +
          scale_color_manual(values=c("grey", "orange", "red", "purple", "black")) +
          geom_vline(xintercept=c(-1, 1), col="red") +
          geom_hline(yintercept=-log10(0.05), col="red")
}
recompute <- 1
if(recompute){

  ggplot(data=results, aes(x=MLSeq, y=-log10(pvalue))) +
          geom_point() +
          theme_minimal() +
          #geom_text_repel() +
          scale_color_manual(values=c("grey", "orange", "red", "purple")) +
          geom_vline(xintercept=c(-1, 1), col="red") +
          geom_hline(yintercept=-log10(0.05), col="red")
}
```

MAIT
```{r}
recompute <- 1
if(recompute){

  results <- read.csv(file.path(dir_MAITD, "08_results_K12_p01_no_exclusion"))
  results
  load(file.path(dir_MAITM, "07_pam.rda"))
  load(file.path(dir_MAITM, "08_plda.rda"))
  load(file.path(dir_MAITM, "09_plda2.rda"))
  load(file.path(dir_MAITM, "10_vnsc.rda"))


  genes <- genes.pam
  #create new column with 0
  results$MLSeq <- 0
  #iterate through all genes
  start <- Sys.time()

  for(gene in genes){

    #if genes.pam contains gene, set to 1
    print(gene)
    pos <- which(results == gene)
    results[pos,8] <- results[pos,8] + 1
  }
  print("1/4")
  print(Sys.time()-start)
  genes <- genes.plda
  for(gene in genes){
    print(gene)
    pos <- which(results == gene)
    results[pos,8] <- results[pos,8] + 1
  }
  print("2/4")



  print(Sys.time()-start)
  genes <- genes.plda2

  for(gene in genes){
    print(gene)
    pos <- which(results == gene)
    results[pos,8] <- results[pos,8] + 1
  }
  print("3/4")
  print(Sys.time()-start)
  genes <- genes.vnsc

  for(gene in genes){
    print(gene)
    pos <- which(results == gene)
    results[pos,8] <- results[pos,8] + 1
  }
  print("4/4")
  print(Sys.time()-start)

  results$MLSeq <- as.factor(results$MLSeq)
  results[1, 6] <- 2.23e-308

  save(results, file = file.path(results_dir, "MAIT_table.Rdata"))
} else{
  load(file = file.path(results_dir, "MAIT_table.Rdata"))
}
```
plot MAIT data

```{r}
if(recompute){
  #jpeg(file = file.path(results_dir, "MAIT_volcano"))
  ggplot(data=results, aes(x=log2FoldChange, y=-log10(pvalue), col=MLSeq)) +
          geom_point() +
          theme_minimal() +
          #geom_text_repel() +
          scale_color_manual(values=c("grey", "orange", "red", "purple", "black")) +
          geom_vline(xintercept=c(-1, 1), col="red") +
          geom_hline(yintercept=-log10(0.05), col="red")
  #dev.off()

  gg <- ggplot(data=results, aes(x=log2FoldChange, y=-log10(pvalue), col=MLSeq)) +
          geom_point() +
          theme_minimal() +
          #geom_text_repel() +
          scale_color_manual(values=c("grey", "orange", "red", "purple", "black")) +
          geom_vline(xintercept=c(-1, 1), col="red") +
          geom_hline(yintercept=-log10(0.05), col="red")


  print("first plot done")

  gg2 <- ggplot(data=results, aes(x=MLSeq, y=-log10(pvalue))) +
          geom_point() +
          theme_minimal() +
          #geom_text_repel() +
          scale_color_manual(values=c("grey", "orange", "red", "purple")) +
          geom_vline(xintercept=c(-1, 1), col="red") +
          geom_hline(yintercept=-log10(0.05), col="red")
  print("second plot done")
}
```
